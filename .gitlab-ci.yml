stages:
    - build

default:
    image: schickling/latex

build:
    stage: build
    
    script:
        - bash build.sh
    
    artifacts:
        paths:
            - build/*.pdf
        expire_in: 1 month